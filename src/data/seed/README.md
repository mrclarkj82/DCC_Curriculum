# Seed Data Copies

These JSON files mirror `curriculum/website-data` for the Phase 3 scaffold preview.

The app imports these local copies so Vite can build without coupling runtime code directly to the curriculum source folders. A later Firebase/Firestore phase should replace this loader with Firestore-backed data access and keep curriculum files as the source of truth.

