rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Placeholder rules for Phase 1. Replace when assignment and media uploads are implemented.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

