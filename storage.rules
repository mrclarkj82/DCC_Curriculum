rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Placeholder rules for Phase 3.
    // Assignment and media upload rules will be implemented in a later phase.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

