rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder rules for Phase 1. Replace during the Firebase implementation phase.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

